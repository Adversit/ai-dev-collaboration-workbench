# 2026-09-20 画布修复与账号存储

本次范围：修正分层画布，接入 Sites dispatch-owned ChatGPT 登录与按用户隔离的 D1 存储。不含管理员跨用户查询。

## 已执行检查

- Node 自动化测试 20 项通过（原有 10 项、画布几何 5 项、服务端认证与存储 5 项）。
- 浏览器：三模块同时展开，5 个子模块和 4 条真实数据关系；内部上下箭头可见。
- 浏览器：进入事件处理仅显示该容器和 2 个子模块；返回后恢复进入前的 camera。
- 浏览器：拖动父模块时，2 个子模块跟随同一位移；收起后隐藏子模块并重新投影关系。
- SQLite 测试执行实际迁移 SQL 与实际服务端查询：匿名拒绝，A/B 账号隔离，数据往返，版本冲突，跨域写入拒绝，非法数据拒绝。
- 登录由 Sites 平台负责，不保存密码、ChatGPT token，不读取聊天记录，不调用 GPT API。

## 限制与待确认

- 正式站点的 ChatGPT OAuth 跳转和真实账号跨设备读取需上线后由用户确认；本地浏览器未进行真实账号登录。
- 管理员跨用户数据读取未实现，API 仅按当前登录用户 ID 查询。
- 本次未对整个 v1.1 需求书重新全面验收；旧版验收历史、开发包等问题不在本次画布修复验收范围。
- 旧浏览器项目不会静默归入第一个登录账号；用户在“我的账号 → 导入旧浏览器项目”明确导入。原备份保留。
- 项目云端保存失败时保留页面内修改并提示重试/导出，不声称已保存；多窗口冲突不会自动覆盖另一窗口的数据。
- 自动避让保证根节点不重叠，但不是全局最优图布局或完整障碍物寻路。
